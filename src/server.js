const app = require("./index"); // Importar a instância do Express configurada em index.js
app.listen(5000); //Inicia o servidor na porta 5000,neste caso a Api será acessível em http://localhost:5000/